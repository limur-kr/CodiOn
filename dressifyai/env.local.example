# 이 파일을 참고해서 프로젝트 루트에 ".env.local"을 만들어 값만 채우세요.
# (보안상 .env.local은 커밋하지 않는 것을 권장합니다)

# (옵션) 프론트에서 Gemini를 직접 호출할 때만 필요합니다. (보안상 권장 X)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# (권장) n8n을 통해 Gemini를 호출할 때 사용합니다. (프론트에는 Webhook URL만 둡니다)
# n8n에서 "Webhook" 노드의 Test URL을 그대로 넣으세요.
VITE_N8N_ANALYZE_IMAGE_WEBHOOK_URL=http://localhost:5678/webhook-test/analyze-clothing
VITE_N8N_OUTFIT_WEBHOOK_URL=http://localhost:5678/webhook-test/generate-outfit

# (테스트 화면: App → AI 연동 메뉴)
VITE_N8N_IMAGE_ANALYZER_WEBHOOK_URL=http://localhost:5678/webhook-test/analyze-image



